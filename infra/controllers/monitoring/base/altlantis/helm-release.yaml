# https://fluxcd.io/flux/components/helm/helmreleases/

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: runatlantis
  namespace: runatlantis
spec:
  releaseName: cert-manager
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: helm-chart-atlantis
    namespace: runatlantis

