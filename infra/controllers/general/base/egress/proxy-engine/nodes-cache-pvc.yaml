apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nodes-cache
  namespace: egress-system
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
